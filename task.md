# 项目描述
BtSimple (mdserver-web) 是一个简单的 Linux 面板，旨在提供高效、简洁的服务器管理体验。

# 开发规范
- 简洁至上 (KISS 原则)
- 统一使用 UTF-8 (无 BOM)
- 优先使用内置函数和已有工具
- 严格遵循构思、审核、拆解、执行流程

# 任务清单
- [x] 修复页面更新报错：服务器数据或网络有问题!
    - [x] 在 `versionDiff` 中支持 `v` 前缀
    - [x] 在 `updateServer` 中改用 `tag_name` 替代 `name`
- [x] 优化安装/迁移完成后的信息展示
    - [x] 在 `deploy.sh` 和 `mw.tpl` 中增加版本号显示
    - [x] 自动忽略版本号中的 `v` 前缀
- [x] 优化防火墙面板 UI
    - [x] 在新增规则表单前增加“新增规则：”提示文字
    - [x] 重构端口输入：支持双输入框、自动同步和范围校验
    - [x] 增加端口和备注搜索过滤功能（支持范围匹配）
    - [x] 为规则列表添加表头
    - [x] 防火墙标签化重构（端口防火墙/IP防火墙）
    - [x] 实现 IP 全局放行/禁止功能及高优先级处理
    - [x] 增加 IP 防火墙操作的安全风险警示与客户端 IP 拦截
    - [x] 为 IP 防火墙增加独立的 IP 和备注搜索过滤功能
- [x] 实现文件拖拽上传功能
    - [x] 在 `files.html` 中添加拖拽区域和覆盖层
    - [x] 在 `files.js` 中实现拖拽事件监听与处理
    - [x] 支持文件夹递归读取
    - [x] 实现上传前的确认展示列表
    - [x] 集成现有上传逻辑完成上传
- [x] 菜单增加折叠功能
    - [x] 在 `layout.html` 中添加折叠图标
    - [x] 在 `site.css` 中添加迷你菜单样式
    - [x] 在 `public.js` 中实现折叠切换与状态持久化
- [x] 优化上传文件权限继承逻辑
    - [x] 在 `files.py` 中增加新目录创建时的权限继承调用
    - [x] 在 `utils/file.py` 中修复 `uploadSegment` 的权限设置逻辑
- [x] 完成文件管理标签页功能
    - [x] 在 `files.html` 中优化标签页样式 (Chrome 风格)
    - [x] 在 `files.js` 中重构标签页逻辑，支持当前路径实时同步
    - [x] 实现添加、删除、切换标签页功能
    - [x] 确保标签页信息永久保存及持久化 (localStorage)
- [x] 增强上传确认对话框功能
    - [x] 支持待上传列表手动删除单个项目
    - [x] 整合添加文件和文件夹为统一的“添加项目”下拉按钮
    - [x] 统一工具栏“上传”按钮逻辑，使其直接弹出确认对话框
    - [x] 优化计划任务功能
    - [x] 隐藏首页直接显示的添加任务表单
    - [x] 在任务列表上方添加“添加任务”、“导入”、“导出”按钮
    - [x] 实现“添加任务”模态框，迁移原有的添加表单逻辑
    - [x] 实现任务列表导出功能（JSON格式）
    - [x] 实现任务列表导入功能（支持文件上传解析）
    - [x] 任务列表“添加时间”改为显示“上次执行时间”
- [x] 优化文件管理工具栏布局
    - [x] 确保根目录和快捷路径在同一排显示
    - [x] 调整快捷路径按钮间距（增加空隙）
- [x] 优化计划任务日志模块
    - [x] 移动“清空”按钮到日志框上方
    - [x] 增加日志框高度 100px 并同步扩大模态框
    - [x] 增加“5秒定时刷新”开关功能
    - [x] 执行任务后自动弹出日志框
    - [x] 在日志界面增加“执行任务”按钮
    - [x] 移动“清空日志”按钮到左下角（操作栏左侧）
    - [x] 日志为空时允许直接打开并显示“暂无数据”
- [x] 软件管理列表增加官网和时间列
    - [x] 后端 `plugin.py`: 在 `makePluginInfo` 中增加 `home` 和 `date` 字段
    - [x] 前端 `soft.html`: 增加“官网”和“发布时间”表头
    - [x] 前端 `soft.js`: 渲染官网链接和发布时间列






