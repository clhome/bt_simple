{
    "', {title: '系统优化提示'}, function(index) {\n        layer.close(index);\n        var loadT = layer.msg('正在执行内核及 THP 优化，请稍候...', { icon: 16, time: 0, shade: 0.3 });\n        \n        sysOptPost('apply_opt', {}, function (resp) {\n            layer.close(loadT);\n            if (!resp.status) {\n                layer.msg(resp.msg, { icon: 2 });\n                return;\n            }\n            \n            var rdata = resp;\n            if (typeof resp.data === 'string') {\n                try {\n                    rdata = JSON.parse(resp.data);\n                } catch(e) {}\n            }\n            \n            layer.msg(rdata.msg, { icon: rdata.status ? 1 : 2 });\n            if(rdata.status){\n                setTimeout(function(){\n                    optStatus();\n                }, 1500);\n            }\n        });\n    });\n}\n\nfunction readme() {\n    $('.bgw').removeClass('bgw');\n    $('.bt-w-menu p:eq(1)').addClass('bgw');\n    var con = `": "', {title: '系统优化提示'}, function(index) {\n        layer.close(index);\n        var loadT = layer.msg('正在执行内核及 THP 优化，请稍候...', { icon: 16, time: 0, shade: 0.3 });\n        \n        sysOptPost('apply_opt', {}, function (resp) {\n            layer.close(loadT);\n            if (!resp.status) {\n                layer.msg(resp.msg, { icon: 2 });\n                return;\n            }\n            \n            var rdata = resp;\n            if (typeof resp.data === 'string') {\n                try {\n                    rdata = JSON.parse(resp.data);\n                } catch(e) {}\n            }\n            \n            layer.msg(rdata.msg, { icon: rdata.status ? 1 : 2 });\n            if(rdata.status){\n                setTimeout(function(){\n                    optStatus();\n                }, 1500);\n            }\n        });\n    });\n}\n\nfunction readme() {\n    $('.bgw').removeClass('bgw');\n    $('.bt-w-menu p:eq(1)').addClass('bgw');\n    var con = `",
    "';\n        }\n\n        // 大数字单位格式化\n        function formatLargeNumber(val) {\n            if (!val || isNaN(val)) return val;\n            var num = parseFloat(val);\n            if (num >= 1e18) return (num / 1e18).toFixed(2) + ' E';\n            if (num >= 1e15) return (num / 1e15).toFixed(2) + ' P';\n            if (num >= 1e12) return (num / 1e12).toFixed(2) + ' T';\n            if (num >= 1e9) return (num / 1e9).toFixed(2) + ' G';\n            if (num >= 1e6) return (num / 1e6).toFixed(2) + ' M';\n            if (num >= 1e3) return (num / 1e3).toFixed(2) + ' K';\n            return num;\n        }\n\n        var mem = parseInt(data.mem_mb) || 2048;\n        var memGB = (mem / 1024).toFixed(1);\n        var con = `": "';\n        }\n\n        // 大数字单位格式化\n        function formatLargeNumber(val) {\n            if (!val || isNaN(val)) return val;\n            var num = parseFloat(val);\n            if (num >= 1e18) return (num / 1e18).toFixed(2) + ' E';\n            if (num >= 1e15) return (num / 1e15).toFixed(2) + ' P';\n            if (num >= 1e12) return (num / 1e12).toFixed(2) + ' T';\n            if (num >= 1e9) return (num / 1e9).toFixed(2) + ' G';\n            if (num >= 1e6) return (num / 1e6).toFixed(2) + ' M';\n            if (num >= 1e3) return (num / 1e3).toFixed(2) + ' K';\n            return num;\n        }\n\n        var mem = parseInt(data.mem_mb) || 2048;\n        var memGB = (mem / 1024).toFixed(1);\n        var con = `",
    "* 本插件执行的优化符合绝大部分高性能 Web 及高并发 DB 场景，安全可靠，可放心一键套用。": "* 本插件执行的优化符合绝大部分高性能 Web 及高并发 DB 场景，安全可靠，可放心一键套用。",
    "0); \n                    reason = \"合格标准：≤ \" + expected_tw_buckets + \" (当前内存\" + (mem/1024).toFixed(1) + \"G动态配置)。限制系统中同时保持的 TIME_WAIT 数量上限，过高会耗尽内存。\";\n                    break;\n                case 'thp_enabled': \n                    ok = !isMissing && (v.indexOf('[never]') !== -1 || v === 'never'); \n                    reason = \"合格标准：never 或者 [never]。透明巨页 (THP) 在多数数据库场景下会导致延迟剧增，必须彻底关闭。\";\n                    break;\n            }\n\n            var tip = ok ? (\"【完美】\" + reason) : (\"【需要优化】\" + reason + (isMissing ? \" (当前未设置)\" : \" (当前值未达标)\"));\n            \n            var icon = ok \n                ? '": "0); \n                    reason = \"合格标准：≤ \" + expected_tw_buckets + \" (当前内存\" + (mem/1024).toFixed(1) + \"G动态配置)。限制系统中同时保持的 TIME_WAIT 数量上限，过高会耗尽内存。\";\n                    break;\n                case 'thp_enabled': \n                    ok = !isMissing && (v.indexOf('[never]') !== -1 || v === 'never'); \n                    reason = \"合格标准：never 或者 [never]。透明巨页 (THP) 在多数数据库场景下会导致延迟剧增，必须彻底关闭。\";\n                    break;\n            }\n\n            var tip = ok ? (\"【完美】\" + reason) : (\"【需要优化】\" + reason + (isMissing ? \" (当前未设置)\" : \" (当前值未达标)\"));\n            \n            var icon = ok \n                ? '",
    "= expected_somaxconn); \n                    reason = \"合格标准：≥ \" + expected_somaxconn + \" (当前内存\" + (mem/1024).toFixed(1) + \"G动态配置)。更高的 TCP 监听队列上限有助于应对高并发连接。\";\n                    break;\n                case 'vm_max_map_count': \n                    ok = !isMissing && (parseInt(v) >= 262144); \n                    reason = \"合格标准：≥ 262144。增加单个进程可拥有的最大 VMA 数量，是 Elasticsearch 等中间件运行的刚需。\";\n                    break;\n                case 'fs_file_max': \n                    ok = !isMissing && (parseInt(v) >= expected_file_max); \n                    reason = \"合格标准：≥ \" + expected_file_max + \" (当前内存\" + (mem/1024).toFixed(1) + \"G动态配置)。极大地扩大系统级文件描述符数量上限。\";\n                    break;\n                case 'net_ipv4_tcp_tw_reuse': \n                    ok = !isMissing && (v == '1'); \n                    reason = \"合格标准：等于 1。允许复用处于 TIME-WAIT 状态的套接字，对高频短连接服务器尤为关键。\";\n                    break;\n                case 'net_ipv4_ip_local_port_range': \n                    ok = !isMissing && (v.indexOf('1024') !== -1 && v.indexOf('65000') !== -1); \n                    reason = \"合格标准：包含 1024 到 65000。释放更多本地动态端口，支持建立海量向外连接。\";\n                    break;\n                case 'net_ipv4_tcp_max_syn_backlog': \n                    ok = !isMissing && (parseInt(v) >= expected_syn_backlog); \n                    reason = \"合格标准：≥ \" + expected_syn_backlog + \" (当前内存\" + (mem/1024).toFixed(1) + \"G动态配置)。增加 SYN 队列长度，防范轻微 SYN 攻击。\";\n                    break;\n                case 'net_ipv4_tcp_max_tw_buckets': \n                    ok = !isMissing && (parseInt(v)": "= expected_somaxconn); \n                    reason = \"合格标准：≥ \" + expected_somaxconn + \" (当前内存\" + (mem/1024).toFixed(1) + \"G动态配置)。更高的 TCP 监听队列上限有助于应对高并发连接。\";\n                    break;\n                case 'vm_max_map_count': \n                    ok = !isMissing && (parseInt(v) >= 262144); \n                    reason = \"合格标准：≥ 262144。增加单个进程可拥有的最大 VMA 数量，是 Elasticsearch 等中间件运行的刚需。\";\n                    break;\n                case 'fs_file_max': \n                    ok = !isMissing && (parseInt(v) >= expected_file_max); \n                    reason = \"合格标准：≥ \" + expected_file_max + \" (当前内存\" + (mem/1024).toFixed(1) + \"G动态配置)。极大地扩大系统级文件描述符数量上限。\";\n                    break;\n                case 'net_ipv4_tcp_tw_reuse': \n                    ok = !isMissing && (v == '1'); \n                    reason = \"合格标准：等于 1。允许复用处于 TIME-WAIT 状态的套接字，对高频短连接服务器尤为关键。\";\n                    break;\n                case 'net_ipv4_ip_local_port_range': \n                    ok = !isMissing && (v.indexOf('1024') !== -1 && v.indexOf('65000') !== -1); \n                    reason = \"合格标准：包含 1024 到 65000。释放更多本地动态端口，支持建立海量向外连接。\";\n                    break;\n                case 'net_ipv4_tcp_max_syn_backlog': \n                    ok = !isMissing && (parseInt(v) >= expected_syn_backlog); \n                    reason = \"合格标准：≥ \" + expected_syn_backlog + \" (当前内存\" + (mem/1024).toFixed(1) + \"G动态配置)。增加 SYN 队列长度，防范轻微 SYN 攻击。\";\n                    break;\n                case 'net_ipv4_tcp_max_tw_buckets': \n                    ok = !isMissing && (parseInt(v)",
    "SYN backlog 大小": "SYN backlog 大小",
    "TCP监听队列上限": "TCP监听队列上限",
    "THP (透明巨页)": "THP (透明巨页)",
    "TIME-WAIT socket 复用": "TIME-WAIT socket 复用",
    "TIME_WAIT 保持上限": "TIME_WAIT 保持上限",
    "`;\n        $('.soft-man-con').html(con);\n\n        // 尝试自动拉宽所在的 layui-layer 弹窗，提供更好的视觉体验\n        var layuiLayer = $('.soft-man-con').closest('.layui-layer');\n        if(layuiLayer.length > 0 && layuiLayer.width()": "`;\n        $('.soft-man-con').html(con);\n\n        // 尝试自动拉宽所在的 layui-layer 弹窗，提供更好的视觉体验\n        var layuiLayer = $('.soft-man-con').closest('.layui-layer');\n        if(layuiLayer.length > 0 && layuiLayer.width()",
    "`;\n    $('.soft-man-con').html(con);\n}\n\n// 初始化加载\noptStatus();": "`;\n    $('.soft-man-con').html(con);\n}\n\n// 初始化加载\noptStatus();",
    "resetPluginWinHeight(580);\n\nfunction sysOptPost(method, args, callback) {\n    var req_data = {\n        name: 'linux_sys_opt',\n        func: method,\n        version: '',\n        args: JSON.stringify(args || {})\n    };\n\n    $.post('/plugins/run', req_data, function(data) {\n        if(typeof(callback) == 'function'){\n            callback(data);\n        }\n    }, 'json').fail(function() {\n        layer.msg('请求失败', {icon: 2});\n    });\n}\n\nfunction optStatus() {\n    $('.bgw').removeClass('bgw');\n    $('.bt-w-menu p:eq(0)').addClass('bgw');\n    var loadT = layer.msg('正在获取服务器参数状态...', { icon: 16, time: 0, shade: 0.3 });\n    \n    sysOptPost('get_status', {}, function (resp) {\n        layer.close(loadT);\n        if (!resp.status) {\n            layer.msg(resp.msg, { icon: 2 });\n            return;\n        }\n        \n        // resp.data is often a JSON string returned by python's returnJson\n        var rdata = resp;\n        if (typeof resp.data === 'string') {\n            try {\n                rdata = JSON.parse(resp.data);\n            } catch(e) {}\n        }\n        \n        var data = rdata.data || rdata;\n\n        // 评分函数（返回完整 TD 标签，使悬浮范围扩大到整个单元格）\n        function getScoreTD(key, val) {\n            var v = String(val).trim();\n            var isMissing = (val === undefined || val === null || val === '未设置');\n            var ok = false;\n            var reason = \"\";\n            var mem = parseInt(data.mem_mb) || 2048;\n            \n            // 动态判定阈值\n            var expected_somaxconn = (mem": "resetPluginWinHeight(580);\n\nfunction sysOptPost(method, args, callback) {\n    var req_data = {\n        name: 'linux_sys_opt',\n        func: method,\n        version: '',\n        args: JSON.stringify(args || {})\n    };\n\n    $.post('/plugins/run', req_data, function(data) {\n        if(typeof(callback) == 'function'){\n            callback(data);\n        }\n    }, 'json').fail(function() {\n        layer.msg('请求失败', {icon: 2});\n    });\n}\n\nfunction optStatus() {\n    $('.bgw').removeClass('bgw');\n    $('.bt-w-menu p:eq(0)').addClass('bgw');\n    var loadT = layer.msg('正在获取服务器参数状态...', { icon: 16, time: 0, shade: 0.3 });\n    \n    sysOptPost('get_status', {}, function (resp) {\n        layer.close(loadT);\n        if (!resp.status) {\n            layer.msg(resp.msg, { icon: 2 });\n            return;\n        }\n        \n        // resp.data is often a JSON string returned by python's returnJson\n        var rdata = resp;\n        if (typeof resp.data === 'string') {\n            try {\n                rdata = JSON.parse(resp.data);\n            } catch(e) {}\n        }\n        \n        var data = rdata.data || rdata;\n\n        // 评分函数（返回完整 TD 标签，使悬浮范围扩大到整个单元格）\n        function getScoreTD(key, val) {\n            var v = String(val).trim();\n            var isMissing = (val === undefined || val === null || val === '未设置');\n            var ok = false;\n            var reason = \"\";\n            var mem = parseInt(data.mem_mb) || 2048;\n            \n            // 动态判定阈值\n            var expected_somaxconn = (mem",
    "⚡ 核心亮点：动态自适应架构": "⚡ 核心亮点：动态自适应架构",
    "一键全局优化": "一键全局优化",
    "一键化全平台内核参数与并发优化": "一键化全平台内核参数与并发优化",
    "一键彻底禁用 Transparent Huge Pages (THP)，底层根治 Redis、MySQL 等数据库的周期性延迟绝症。": "一键彻底禁用 Transparent Huge Pages (THP)，底层根治 Redis、MySQL 等数据库的周期性延迟绝症。",
    "不同内存阶梯配置": "不同内存阶梯配置",
    "中间件专供：": "中间件专供：",
    "优先使用物理内存": "优先使用物理内存",
    "优化状态": "优化状态",
    "作用描述": "作用描述",
    "全面拉升 TCP 监听队列上限，优化 TIME_WAIT 套接字复用及释放海量本地端口。": "全面拉升 TCP 监听队列上限，优化 TIME_WAIT 套接字复用及释放海量本地端口。",
    "内核与并发参数状态": "内核与并发参数状态",
    "参数名": "参数名",
    "外连端口范围": "外连端口范围",
    "大幅提升单个进程允许的最大内存映射区域数，原生完美兼容 Elasticsearch 等重型数据引擎。": "大幅提升单个进程允许的最大内存映射区域数，原生完美兼容 Elasticsearch 等重型数据引擎。",
    "平滑刷盘，防止IO卡顿": "平滑刷盘，防止IO卡顿",
    "应用后立即生效，此操作将提升高并发及数据库运行性能。": "应用后立即生效，此操作将提升高并发及数据库运行性能。",
    "开启可能导致数据库卡顿": "开启可能导致数据库卡顿",
    "强杀透明巨页：": "强杀透明巨页：",
    "当前值": "当前值",
    "当前内存为：": "当前内存为：",
    "彻底告别“一刀切”的危险操作。脚本会根据您的服务器真实物理内存自动推演并下发最优的网络并发及句柄容量。": "彻底告别“一刀切”的危险操作。脚本会根据您的服务器真实物理内存自动推演并下发最优的网络并发及句柄容量。",
    "御风 Linux 核心引擎优化": "御风 Linux 核心引擎优化",
    "御风系统优化": "御风系统优化",
    "打破文件束缚：": "打破文件束缚：",
    "按内存比例自适应扩大系统级文件描述符上限（fs.file-max），永久告别": "按内存比例自适应扩大系统级文件描述符上限（fs.file-max），永久告别",
    "插件说明": "插件说明",
    "既能防止低配机器 OOM 死机，也能彻底释放高配机器的极限吞吐能力。": "既能防止低配机器 OOM 死机，也能彻底释放高配机器的极限吞吐能力。",
    "智能内存管控：": "智能内存管控：",
    "最大VMA数量": "最大VMA数量",
    "未知": "未知",
    "未设置": "未设置",
    "本插件整合了业界最严苛的生产环境最佳实践，专门针对标准 Linux 系统（涵盖 CentOS / RHEL / Debian / Ubuntu 等体系）进行深度自适应性能调优。": "本插件整合了业界最严苛的生产环境最佳实践，专门针对标准 Linux 系统（涵盖 CentOS / RHEL / Debian / Ubuntu 等体系）进行深度自适应性能调优。",
    "极限网络承载：": "极限网络承载：",
    "系统最大文件描述符": "系统最大文件描述符",
    "自动匹配": "自动匹配",
    "衢州御风科技有限公司出品": "衢州御风科技有限公司出品",
    "评分": "评分",
    "降低 Swap 倾向以防止 OOM，改善磁盘 IO 队列及脏页刷新平滑度。": "降低 Swap 倾向以防止 OOM，改善磁盘 IO 队列及脏页刷新平滑度。",
    "限制脏页比例上限": "限制脏页比例上限",
    "，优化脚本会": "，优化脚本会",
    "🛡️ 深度优化清单：": "🛡️ 深度优化清单："
}